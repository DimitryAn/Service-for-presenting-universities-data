from pathlib import Path

PROJECT_DIR = Path(__file__).parent
INITIAL_DATA_DIR = PROJECT_DIR / "data" / "initial data"
MODIFIED_DATA_DIR = PROJECT_DIR / "data" / "modified data"
DB_PATH = PROJECT_DIR / "data" / "universities.db"
